# Fashion AI Engine 🚀

基于 **Next.js** + **Vercel AI SDK** + **OpenRouter** 的服装电商自动化营销中枢。

## 架构说明 (Architecture)

本项目作为“大脑”，通过统一的接口标准 (Vercel AI SDK) 调度全球最强的 AI 模型。

### 核心技术栈
- **Framework**: Next.js 14 (App Router)
- **AI SDK**: Vercel AI SDK (`ai`, `@ai-sdk/openai`)
- **Model Gateway**: [OpenRouter](https://openrouter.ai/)
    - 文本/多模态: `google/gemini-2.0-flash-exp:free` (推荐，速度快且免费)
    - 图像生成: 可通过 OpenRouter 调用 `black-forest-labs/flux-1-schnell` 等模型。
- **UI**: Tailwind CSS

### 目录结构
- `src/lib/ai-config.ts`: OpenRouter 的统一配置，在这里切换模型。
- `src/app/actions.ts`: 服务端 Actions，包含核心的 Prompt 工程和工作流逻辑。
- `src/app/page.tsx`: 前端交互界面。

## 快速开始

1. **安装依赖**
   ```bash
   npm install
   ```

2. **配置环境变量**
   复制 `.env.local.example` 为 `.env.local` 并填入您的 API Key。
   ```bash
   cp .env.local.example .env.local
   ```
   *获取 Key: [OpenRouter Keys](https://openrouter.ai/keys)*

3. **启动开发服务器**
   ```bash
   npm run dev
   ```

## 扩展指南 (Integration)

### 1. 接入 InfiniteTalk (视频生成)
目前视频生成模块 `InfiniteTalk` 建议作为独立的 API 服务运行（如部署在 RunPod）。
在 `src/app/actions.ts` 中，您可以解除 `generateVideo` 函数的注释，并使用 `fetch()` 调用您的 RunPod API。

### 2. 接入虚拟试穿 (VTON)
对于复杂的图像处理，建议使用专门的 API (如 SiliconFlow)。
虽然 OpenRouter 支持部分图像模型，但专业的 VTON (Virtual Try-On) 通常需要专用 API。

---
Generated by Gemini CLI Agent